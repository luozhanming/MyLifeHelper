fragment UserObject on User{
             id
             login
     		 name
             avatarUrl(size:64)
             bio
             bioHTML
             company
             companyHTML
             email
             location
             url
             websiteUrl
     		 createdAt
     		 isViewer
     		  starredRepositories(first:0){
                 totalCount
               }
     		  following(first:0){
                 totalCount
               }
              followers(first:0){
                 totalCount
               }
            status{
                id
                emoji
                message
            }

}
